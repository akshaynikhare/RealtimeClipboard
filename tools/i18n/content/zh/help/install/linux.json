{
  "title": "在 Linux 上安装 RealtimeClipboard",
  "desc": "Debian、Ubuntu、Fedora 和 Arch 上怎么装，以及 Wayland 后台捕获：KDE 和 wlroots 行，GNOME 不行。",
  "ogTitle": "在 Linux 上安装 RealtimeClipboard",
  "ogDesc": ".deb、.rpm 和 AppImage，webkit2gtk 那条底线，以及哪些合成器允许后台捕获剪贴板。",
  "headline": "在 Linux 上安装 RealtimeClipboard —— .deb、.rpm 和 AppImage",
  "schemaDesc": "在 Debian、Ubuntu、Fedora 和 Arch 上的安装方法，以及把 Wayland 那条限制直说清楚：X11 可以，KDE 和 wlroots 可以，GNOME 不行。",
  "crumb": "在 Linux 上安装",
  "h1": "<svg class=\"brandmark\" viewBox=\"0 0 24 24\" aria-hidden=\"true\"><use href=\"#i-linux\"/></svg> 在 Linux 上安装",
  "lede": "          三种包格式，外加一条值得你在装任何东西之前先读一遍的限制：后台捕获\n          剪贴板在这里能不能用，是由你的合成器决定的，不是由这个应用决定的。",
  "body": "\n        <h2 id=\"install\">安装</h2>\n        <p>从<a href=\"https://github.com/akshaynikhare/RealtimeClipboard/releases/latest\">发布页</a>取一个和你的发行版对得上的文件：</p>\n        <pre><code># Debian、Ubuntu 及其衍生版\nsudo apt install ./RealtimeClipboard_&lt;version&gt;_amd64.deb\n\n# Fedora、RHEL 及其衍生版\nsudo dnf install ./RealtimeClipboard-&lt;version&gt;-1.x86_64.rpm\n\n# 其他任何情况\nchmod +x RealtimeClipboard_&lt;version&gt;_amd64.AppImage\n./RealtimeClipboard_&lt;version&gt;_amd64.AppImage</code></pre>\n        <p>\n          <b>AppImage 那个 <code>chmod</code> 少不了。</b>用浏览器下载会清掉可执行位，\n          而由此产生的故障是：你双击之后文件管理器什么都不做 —— 看起来完全不像是\n          权限问题。\n        </p>\n        <p>\n          <b>发行版底线：</b>应用需要 webkit2gtk 4.1，大致相当于 Ubuntu 22.04 或\n          Fedora 36 及以上。发布版本是特意挑有它的最旧 runner 构建的，好把 glibc 的\n          要求压到最低。比这更老的系统，请用浏览器或者<a href=\"/zh/help/install/cli/\">命令行客户端</a>。\n        </p>\n\n        <h2 id=\"pkg\">这里没有包管理器</h2>\n        <p>\n          没有 Flathub，没有 Snap，没有 AUR —— 这一页也不会写个命令假装有。这些各自\n          都是一个得有人跟着每次发布去更新的仓库，而一个悄悄停止更新的仓库比压根不\n          存在还糟：它递给别人一个旧版本，还告诉人家这是最新的。\n        </p>\n        <p>\n          上面那三个文件就是全部。每一个都是发布里的单个产物，谁都不需要仓库，\n          AppImage 尤其什么都不用装 —— <code>chmod +x</code>，然后运行。\n        </p>\n\n        <h2 id=\"check\">确认装好了</h2>\n        <ul class=\"sq-list\">\n          <li><b>托盘里出现一个图标</b>，前提是你的桌面环境有托盘 —— GNOME 得装一个\n            扩展才能显示托盘图标，那是 GNOME 的决定，和这个应用无关。\n            <b>右键点它打开菜单</b>：在 Linux 上，托盘图标收不到属于自己的点击，\n            所以交互就只有这一种。<b>Open</b> 和 <b>Quit</b> 都在里面。关掉窗口不等于\n            退出 —— 有没有托盘图标，窗口都照常工作，\n            <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>V</kbd> 也照常。</li>\n          <li><b>右下角的状态栏才是真正的答案。</b>在后台捕获可用的地方，它显示\n            <code>T0 · watching the system clipboard</code>。不可用的地方，它会直说，\n            而不是默默地什么都不做。</li>\n        </ul>\n\n        <h2 id=\"wayland\">Wayland 那条限制，直说</h2>\n        <div class=\"tablewrap\">\n          <table class=\"cmp\">\n            <thead>\n              <tr><th>会话</th><th>后台捕获</th></tr>\n            </thead>\n            <tbody>\n              <tr><td>X11，任意桌面</td><td class=\"y\">可以</td></tr>\n              <tr><td>Wayland，KDE Plasma</td><td class=\"y\">可以</td></tr>\n              <tr><td>Wayland，wlroots（Sway、Hyprland、river）</td><td class=\"y\">可以</td></tr>\n              <tr><td>Wayland，GNOME</td><td class=\"n\">不行 —— 改为在窗口获得焦点时捕获</td></tr>\n            </tbody>\n          </table>\n        </div>\n        <p>\n          在 X11 上，剪贴板一变，应用就会收到通知。在 Wayland 上，这完全取决于合成器：\n          KDE 和基于 wlroots 的那些提供了对应的协议，GNOME 没有。<b>在 GNOME Wayland\n          上，任何应用都无法监听剪贴板</b> —— 这不是这个应用缺了什么功能，别的工具\n          也一样没能解决。\n        </p>\n        <p>\n          在用不了的地方，应用退回到你切到它时再捕获，跟浏览器里的行为一模一样，并且\n          在状态栏里如实说明。如果后台捕获正是你要装它的理由，那么在 GNOME 上，答案\n          是改用 X11 会话。\n        </p>\n\n        <h2 id=\"first\">第一次同步</h2>\n        <ol class=\"steps\">\n          <li>记下应用顶部那个短密钥，或者自己输一个。</li>\n          <li>在第二台设备上打开<a href=\"/app.html\">应用</a>，输入同一个密钥。</li>\n          <li>复制点什么。如果状态栏写的是 <code>T0</code>，你不需要切换窗口；如果不是，\n            点一下 RealtimeClipboard 窗口，内容会在它获得焦点的那一刻被取走。</li>\n        </ol>\n        <p class=\"honest\">\n          窗口顶部那个开关默认在 <b>Clipboard</b>（剪贴板），也就是说你复制的每一样\n          东西都会被共享。它在运行期间会读取你的每一次复制，这件事值得你知道，而不是\n          稀里糊涂地继承下来 —— 应用在第一次启动时就会说明，而切到 <b>App</b>（应用）\n          之后，你不把内容粘贴进来，就没有东西离开这台机器。\n        </p>\n\n        <h2 id=\"trouble\">不工作的时候</h2>\n\n        <h3>双击 AppImage 没有任何反应</h3>\n        <p>给它 <code>chmod +x</code> —— 见上面。在终端里跑一次，你看到的就是真正的\n          报错，而不是一片安静。</p>\n\n        <h3>安装时报缺少库</h3>\n        <p>\n          <code>.deb</code> 声明了 <code>libwebkit2gtk-4.1-0</code>、\n          <code>libgtk-3-0</code> 和\n          <code>libayatana-appindicator3-1 | libappindicator3-1</code>；\n          用 <code>apt install ./file.deb</code> 而不是 <code>dpkg -i</code> 来装，\n          依赖会帮你解决好。最后那个是画托盘图标的，写成一个“或”的关系是因为：较新的\n          Debian 里已经没有那个普通包，而一些较老的发行版里又没有 ayatana 那个 ——\n          你的发行版会有其中之一。至于老到连 webkit2gtk 4.1 都没有的发行版，那就是\n          上面说的那条底线，没有变通办法。\n        </p>\n\n        <h3>复制没有任何反应，状态栏也不显示 T0</h3>\n        <p>那是 Wayland 那张表，不是故障。见上面。</p>\n\n        <h3>一直停在 “Connecting…”</h3>\n        <p>\n          应用先试 WebSocket，在升级被挡住的地方回退到 server-sent events 加 POST。\n          如果两条都连不上，那就是中继服务器的域名被封了 —— 在 <b>Settings → Relay</b>\n          里把应用指向你自己的一台。\n          <a href=\"/zh/help/install/relay/\">自己跑一个只要一个容器。</a>\n        </p>\n\n        <h2 id=\"remove\">卸载</h2>\n        <pre><code>sudo apt remove realtimeclipboard      # or dnf remove\nyay -R realtimeclipboard-bin</code></pre>\n        <p>\n          AppImage 就是一个文件：删掉就行。别的什么都不会留下 —— 剪贴板内容从来不会\n          写到磁盘上，你的设置和剪贴历史都存在应用自己的浏览器存储里。\n        </p>\n\n        <p class=\"hint\">\n          准备好安装了：<a href=\"/zh/download/\">下载页</a>。其他所有平台：\n          <a href=\"/zh/help/install/\">安装指南</a>。这里有写错或者过时的地方？\n          <a href=\"https://github.com/akshaynikhare/RealtimeClipboard/issues\">提一个 issue</a>。\n        </p>",
  "faq": "",
  "note": "\n  Every id on this page is a deep-link target used from /help/install/ and from\n  the other platform guides (#install, #pkg, #check, #wayland, #first, #trouble,\n  #remove). They are byte-identical to the English original and must stay that\n  way; #wayland in particular is linked from the hub.\n\n  Every command inside pre and code is byte-identical to the English: those\n  blocks carry a copy button, and a translated command is a command that fails\n  when pasted. Only the standalone comment lines in the install block are\n  translated, and the trailing comment on the apt remove line is left alone\n  because the line itself is the command. The status-bar strings sit in code for\n  the same reason, and because the desktop app is not localised.\n\n  The Wayland table is the point of the page and its four rows keep the English\n  order and the y/n classes that colour them.\n\n  Terminology follows /zh/: 剪贴板, 密钥, 中继服务器, 扩展, 安装, 卸载.\n"
}

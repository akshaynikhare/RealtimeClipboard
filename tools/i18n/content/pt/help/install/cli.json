{
  "title": "Linha de comando: área de transferência via SSH",
  "desc": "Um cliente de linha de comando para máquinas sem área de transferência. Canalize para send, redirecione o watch e use a criptografia do navegador.",
  "ogTitle": "RealtimeClipboard na linha de comando",
  "ogDesc": "Canalize sua área de transferência para dentro e para fora de qualquer máquina com Node 22. A mesma criptografia e o mesmo protocolo do navegador.",
  "headline": "O cliente de linha de comando do RealtimeClipboard — área de transferência via SSH",
  "schemaDesc": "Um cliente de linha de comando para máquinas sem área de transferência. Canalize para send, redirecione o watch e use a mesma criptografia do navegador. Precisa do Node 22.",
  "crumb": "Linha de comando",
  "h1": "<svg class=\"brandmark\" viewBox=\"0 0 24 24\" aria-hidden=\"true\"><use href=\"#i-node\"/></svg> O cliente de linha de comando",
  "lede": "          Para as máquinas que não têm área de transferência nenhuma. Ele lê a\n          entrada padrão e escreve na saída padrão, então se encaixa em tudo o que\n          você já usa — e não é uma segunda implementação: importa exatamente os\n          mesmos arquivos de criptografia e transporte que o navegador executa.",
  "body": "\n        <h2 id=\"install\">Instale</h2>\n        <pre><code>npx realtimeclipboard D75LV</code></pre>\n        <p>\n          Não há nada para instalar: o <code>npx</code> baixa, executa e não deixa\n          nada para trás. Se preferir tê-lo no seu PATH:\n        </p>\n        <pre><code>npm install -g realtimeclipboard</code></pre>\n        <p>\n          <b>É preciso Node 22 ou mais novo</b>, por causa do\n          <code>WebSocket</code> embutido. Num Node mais antigo o cliente sai na\n          hora e diz o motivo, em vez de falhar no meio de uma transferência.\n        </p>\n\n        <h2 id=\"pkg\">Ou por um gerenciador de pacotes</h2>\n        <pre><code>brew install akshaynikhare/tap/realtimeclipboard-cli</code></pre>\n        <p>\n          Repare no nome: a formula do CLI é <code>realtimeclipboard-cli</code> e o\n          aplicativo de desktop é o cask <code>realtimeclipboard</code>. São coisas\n          diferentes, e instalar uma não te dá a outra — um servidor quer o pipe,\n          não um ícone na barra de menus.\n        </p>\n\n        <h2 id=\"check\">Confira se funcionou</h2>\n        <pre><code>npx realtimeclipboard --version</code></pre>\n        <p>\n          Ao conectar, ele imprime uma linha de status na <b>saída de erro</b>,\n          nunca na saída padrão:\n        </p>\n        <pre><code>realtimeclipboard · room 3f2a91c4…</code></pre>\n        <p>\n          Esse é o hash da sala, truncado — nunca a chave. Como vai para o stderr,\n          <code>watch &gt; file</code> continua gravando só o conteúdo. Passe\n          <code>-q</code> para silenciar de vez.\n        </p>\n\n        <h2 id=\"first\">Sua primeira sincronização</h2>\n        <ol class=\"steps\">\n          <li>Abra <a href=\"/app.html\">o aplicativo</a> no navegador e anote a chave.</li>\n          <li>No servidor, mande alguma coisa para ela:\n            <pre><code>echo \"deploy key\" | npx realtimeclipboard send D75LV</code></pre></li>\n          <li>Aparece no navegador. Agora faça o caminho inverso — copie algo no\n            navegador e, no servidor, rode:\n            <pre><code>npx realtimeclipboard watch D75LV --once &gt; token.txt</code></pre></li>\n        </ol>\n\n        <h2 id=\"usage\">Os quatro comandos</h2>\n        <div class=\"tablewrap\">\n          <table class=\"cmp\">\n            <thead><tr><th>Comando</th><th>O que faz</th></tr></thead>\n            <tbody>\n              <tr><td><code>realtimeclipboard &lt;KEY&gt;</code></td><td>Mão dupla: imprime o que chega, envia o que você digita</td></tr>\n              <tr><td><code>realtimeclipboard watch &lt;KEY&gt;</code></td><td>Imprime o conteúdo que chega</td></tr>\n              <tr><td><code>realtimeclipboard send &lt;KEY&gt;</code></td><td>Lê a entrada padrão até o EOF e envia</td></tr>\n              <tr><td><code>realtimeclipboard new [--long]</code></td><td>Imprime uma chave nova</td></tr>\n            </tbody>\n          </table>\n        </div>\n        <p>Opções: <code>--relay &lt;url&gt;</code>, <code>--pin &lt;pin&gt;</code>,\n          <code>--once</code>, <code>--json</code>, <code>--timeout &lt;s&gt;</code>,\n          <code>-q</code>, <code>-h</code>, <code>-v</code>. O retransmissor e o PIN\n          também são lidos de <code>REALTIMECLIPBOARD_RELAY</code> e\n          <code>REALTIMECLIPBOARD_PIN</code>, que é o jeito certo de passar um PIN —\n          como argumento ele fica no histórico do seu shell.</p>\n\n        <pre><code># saída estruturada, um objeto JSON por linha\nnpx realtimeclipboard watch D75LV --once --json | jq -r .text\n\n# de uma máquina em que você entrou por ssh\nssh build-01 'cat /var/log/deploy.log' | npx realtimeclipboard send D75LV\n\n# apontando para o seu próprio retransmissor\nnpx realtimeclipboard --relay wss://clip.example.com D75LV</code></pre>\n\n        <h2 id=\"trouble\">Quando não funciona</h2>\n\n        <h3>“this needs Node 22 or newer” (precisa do Node 22 ou mais novo)</h3>\n        <p>\n          Exatamente o que está escrito: o cliente usa o <code>WebSocket</code>\n          embutido do Node em vez de puxar uma dependência, e ele só chegou na 22.\n          Ele sai na inicialização, não no meio da transferência.\n        </p>\n\n        <h3>Ele trava e nunca imprime nada</h3>\n        <p>\n          O <code>watch</code> espera por conteúdo, é assim mesmo — nada chega\n          enquanto ninguém copiar nada do outro lado. Use\n          <code>--timeout 30</code> num script para ele não esperar para sempre, e\n          <code>--once</code> para sair depois do primeiro conteúdo.\n        </p>\n\n        <h3>Funciona no terminal, mas não na CI</h3>\n        <p>\n          Não há TTY, então passe <code>-q</code> e leia a saída padrão. Ponha a\n          chave numa variável de ambiente em vez da linha de comando, onde ela\n          apareceria nos logs de build.\n        </p>\n\n        <h3>Nada conecta atrás de um proxy</h3>\n        <p>\n          O cliente fala o mesmo protocolo do navegador e pode ser apontado para o\n          seu próprio retransmissor com <code>--relay</code>.\n          <a href=\"/pt/help/install/relay/\">Rodar um é um contêiner.</a>\n        </p>\n\n        <h2 id=\"remove\">Desinstalando</h2>\n        <p>\n          O <code>npx</code> não deixa nada para desinstalar. Use\n          <code>npm uninstall -g realtimeclipboard</code> se você instalou\n          globalmente, ou <code>brew uninstall realtimeclipboard-cli</code>.\n        </p>\n        <p class=\"honest\">\n          <b>A chave é uma credencial ao portador.</b> Quem estiver com ela\n          consegue ler a sessão enquanto ela estiver aberta, então mantenha a chave\n          fora do histórico do shell, fora dos logs de CI e fora de arquivos\n          versionados. O conteúdo é criptografado nesta máquina antes de sair; o\n          retransmissor só vê um hash de sala e texto cifrado.\n        </p>\n\n        <p class=\"hint\">\n          Todas as outras plataformas: <a href=\"/pt/help/install/\">o guia de\n          instalação</a>. Algo errado ou desatualizado aqui?\n          <a href=\"https://github.com/akshaynikhare/RealtimeClipboard/issues\">Abra uma issue</a>.\n        </p>",
  "faq": "",
  "note": "\n  Every id here is a deep-link target that other pages point at\n  (/help/install/cli/#install, #pkg, #check, #first, #usage, #trouble,\n  #remove), so the ids stay byte-identical to the English original and only the\n  heading text is translated.\n\n  Every pre and code block is the English original unchanged: the npx and npm\n  invocations, the Homebrew formula name, the option list, the environment\n  variable names and the status line the client actually prints. Those blocks\n  carry a copy button and the CLI is not localised, so a translated command\n  would fail the moment it was pasted. Only the three shell comments inside the\n  worked examples are translated, because nothing executes them.\n\n  The heading that quotes the client's startup error keeps the English string\n  the client prints, with a Portuguese gloss after it, so a reader can match\n  what is on their terminal.\n\n  Terminology follows /pt/: área de transferência, chave, retransmissor,\n  criptografia de ponta a ponta, desinstalar.\n"
}
